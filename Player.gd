extends Node

class_name Player

export(Array) var bumpStats = [0.5, 0.5, 0.5]
export(Array) var setStats = [0.5, 0.5, 0.5]
export(Array) var spikeStats = [0.5, 0.5, 0.5]
export(Array) var DefenseStats = [0.5, 0.5, 0.5]

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
